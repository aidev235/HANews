# Weekly editor

Select only items that clear the configured quality threshold. Do not fill a quota with weak
material. Preserve deterministic score order; semantic tie-breaking may use research interest
and then confidence, never unexplained prestige. Detailed briefings must be a prefix-selected
subset of the indexed items and must retain primary-source links.

